#include<opencv2/opencv.hpp>
#include<iostream>

using namespace cv;
/*
int main() {
	Mat src = imread("D:/AI/opencv_learning/test.jpg");
	if (!src.data) {
		printf("could not load");
		return -1;
	}
	imshow("input", src);

	waitKey(0);
	return 0;
}
*/
